{
  "name": "@nxq/runtime-core",
  "version": "1.0.0",
  "private": true,
  "description": "Framework-agnostic NXQ deterministic runtime core — envelope, state machine, identity, leases",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^1.6.0"
  }
}
