{
  "name": "@aceplace/runtime-core",
  "version": "1.0.0",
  "private": true,
  "description": "Framework-agnostic ACEPLACE deterministic runtime core — envelope, state machine, identity, leases",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts",
    "./shared": "./src/shared.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
