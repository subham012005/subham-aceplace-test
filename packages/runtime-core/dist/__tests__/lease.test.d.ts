/**
 * Lease — Unit Tests
 *
 * Validates the per-agent lease model invariants:
 * - Same instance can renew its lease
 * - Different instance triggers FORK_DETECTED
 * - No double lease on same envelope
 */
export {};
//# sourceMappingURL=lease.test.d.ts.map