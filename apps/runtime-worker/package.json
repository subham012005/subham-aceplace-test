{
  "name": "@aceplace/runtime-worker",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "ACEPLACE long-lived deterministic runtime worker — polls execution_queue and drives envelope execution",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aceplace/runtime-core": "*",
    "firebase-admin": "^13.7.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "ts-node": "^10.9.2",
    "typescript": "^5"
  }
}
